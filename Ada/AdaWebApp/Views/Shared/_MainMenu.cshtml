<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="row">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="col-md-4 col-md-offset-3 navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Ada", "Index", "Home", null, new { @class = "navbar-brand" })
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="col-md-4 col-md-offset-1 collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    @{
                        if (HttpContext.Current.User.Identity.IsAuthenticated)
                        {
                            if (HttpContext.Current.User.IsInRole("admin"))
                            {
                                <li>@Html.ActionLink("Admin", "Index", "Person", new { area = "Admin" }, null)</li>
                            }
                            <li>@Html.ActionLink("Conditions d'utilisation", "Index", "CGU")</li>
                            <li>@Html.ActionLink("Log out", "Logout", "Account")</li>
                        }
                        else
                        {
                            <li>@Html.ActionLink("Conditions d'utilisation", "Index", "CGU")</li>
                            <li>@Html.ActionLink("Log in", "Login", "Account")</li>
                        }
                    }
                </ul>
            </div><!-- /.navbar-collapse -->
        </div>
    </div><!-- /.container-fluid -->
</nav>    
